# Cargo configuration for optimized build times
# Copy this file to .cargo/config.toml to enable optimizations

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
